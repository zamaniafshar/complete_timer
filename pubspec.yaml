name: complete_timer
description: A dart timer that can be configured to fire once or repeatedly with ability start, stop, resume and cancel.
version: 1.0.0
homepage: https://github.com/MohammadAminZamaniAfshar/complete_timer

environment:
  sdk: '>=2.17.5 <3.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
